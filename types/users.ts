interface User {
  /**
   * Regsitered user's username
   */
  username: string;
  /**
   * Registered user's password
   */
  password: string;
  /**
   * Registered user's email id
   */
  email: string;
  /**
   * Registered user's mobile number
   */
  mobile: string;
}

export default User;
